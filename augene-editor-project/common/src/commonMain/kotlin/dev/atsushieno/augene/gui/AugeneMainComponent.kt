package dev.atsushieno.augene.gui

/*

import com.arkivanov.decompose.ComponentContext
import com.arkivanov.decompose.RouterState
import com.arkivanov.decompose.value.Value

interface MainUI {
    val routerState: Value<RouterState<*,Child>>

    sealed class Child {
    }
}

interface SettingsUI {

}

class AugeneMainComponent(componentContext: ComponentContext) : ComponentContext by componentContext {
    private val router = router<Any, Any>(initialConfiguration = 0, handleBackButton = true, childFactory = ::createChild)

    fun createChild(config: Config, componentContext: ComponentContext): AugeneMainComponent.
}
*/